extends layouts/main

block content
    .form-container
        form(action="asdf", method="post")
            input(type="text", placeholder="Title", name="title", value=video.value)
            textarea(name="description", placeholder="Description")=video.description
            input(type="submit", value="Update Video")
        a.form-container__link(href="adsf")
            button.delete  Delete Video